name: "S3 Sync"
description: "Sync a directory to an AWS S3 repository and invalidates CloudFront cache"
author: jakejarvis
runs:
  using: docker
  image: Dockerfile
branding:
  icon: refresh-cw
  color: green
